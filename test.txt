pip install mkdocs-pdf-export-plugin weasyprint

/* =====================================================
   FINAL VALIDATED PDF CONFIG – WIDE STAT TABLES
   Tested logic for AUC / CI / OOS / OOT tables
===================================================== */

/* ---------- Page Setup ---------- */

@page {
  size: A4 landscape;
  margin: 12mm;

  @bottom-right {
    content: counter(page);
    font-size: 9px;
  }
}

/* ---------- Print Rules ---------- */

@media print {

  body {
    margin: 0;
  }

  .md-content {
    max-width: 100%;
  }

  /* Hide navigation elements */
  .md-sidebar,
  .md-header,
  .md-footer,
  .md-tabs {
    display: none !important;
  }

  /* Prevent awkward page breaks */
  h1, h2, h3 {
    page-break-after: avoid;
  }

  tr {
    page-break-inside: avoid;
  }

  thead {
    display: table-header-group; /* Repeat header */
  }

  /* ---------- TABLE FIX ---------- */

  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9px;          /* Critical for fitting */
    table-layout: auto;      /* Natural sizing */
  }

  th, td {
    padding: 3px 4px;
    text-align: center;
    white-space: nowrap;     /* Prevent vertical splitting */
  }

  /* Material theme scroll fix */
  .md-typeset__table {
    overflow: visible !important;
  }

}
