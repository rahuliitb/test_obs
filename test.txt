pip install mkdocs-pdf-export-plugin weasyprint

/* =====================================
   CLEAN PDF FIX (NO COLUMN BREAKING)
===================================== */

@media print {

  body {
    margin: 0;
  }

  .md-content {
    max-width: 100%;
  }

  /* Hide navigation */
  .md-sidebar,
  .md-header,
  .md-footer {
    display: none !important;
  }

  /* Better page split */
  h1, h2, h3 {
    page-break-after: avoid;
  }

  p, li {
    page-break-inside: avoid;
  }

  /* Code blocks */
  pre {
    white-space: pre-wrap !important;
  }

  /* ==============================
     TABLE FIX (CORRECT VERSION)
  ============================== */

  table {
    width: 100%;
    border-collapse: collapse;
    page-break-inside: auto;
    table-layout: auto;      /* allow natural width */
    font-size: 11px;         /* ✅ reduce table size */
  }

  th, td {
    white-space: nowrap;     /* prevent vertical word splitting */
  }

  tr {
    page-break-inside: avoid;
  }

  thead {
    display: table-header-group; /* repeat header on new page */
  }

  /* Fix Material scroll wrapper */
  .md-typeset__table {
    overflow: visible !important;
  }
}

/* Optional page numbers */
@page {
  @bottom-right {
    content: counter(page);
    font-size: 10px;
  }
}
